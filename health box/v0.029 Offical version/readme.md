&#x20;我真草了，上周搞了一整周的蓝牙版本，结果这周一来告诉我说要改用云服务
所以说这次要把架构给改成云服务的架构，就是把数据都放到云服务（华为云TOI）上，然后在小程序里调用云服务的
接口。差不多就是这个样子。
然后相对于前面版本的话肯定就是先把原来的的蓝牙架构的代码给删除了，目前云服务的代码还没有深入的写。前端
部分是完成的差不多了，我自己觉得的话可能后续的ui界面风格就定为现在这个样子了。
